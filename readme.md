# Project Setup

This guide will help you set up a Python environment and install the required dependencies for this project.

## 1. Create a Conda Environment

Open your terminal and run:

```bash
conda create -n majorproject python=3.12.12
```

```bash
conda activate majorproject
pip install -r requirements.txt
```
